apiVersion: apps/v1
kind: Deployment
metadata:
  name: stock-sentiment-local
  namespace: crypto-data-collection
  labels:
    app: stock-sentiment-local
    component: data-collector
    market-type: stock
spec:
  replicas: 1
  selector:
    matchLabels:
      app: stock-sentiment-local
  template:
    metadata:
      labels:
        app: stock-sentiment-local
        component: data-collector
        market-type: stock
    spec:
      nodeSelector:
        node-type: data-collection
      tolerations:
        - key: "data-platform"
          operator: "Equal"
          value: "true"
          effect: "NoSchedule"
      containers:
        - name: stock-sentiment-collector
          image: stock-sentiment-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: centralized-db-config
            - secretRef:
                name: centralized-db-secrets
          env:
            - name: MODEL_TYPE
              value: "stock"
            - name: MODEL_NAME
              value: "finbert"
            - name: PORT
              value: "8000"
            - name: LOG_LEVEL
              value: "INFO"
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: 400m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 120
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: stock-sentiment-local
  namespace: crypto-data-collection
  labels:
    app: stock-sentiment-local
    component: data-collector
    market-type: stock
spec:
  selector:
    app: stock-sentiment-local
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP



