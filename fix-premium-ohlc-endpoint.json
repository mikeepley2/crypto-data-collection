{"spec":{"jobTemplate":{"spec":{"template":{"spec":{"containers":[{"name":"premium-ohlc-collection","command":["python","-c","import asyncio\nimport aiohttp\nimport json\nimport logging\n\nlogging.basicConfig(level=logging.INFO)\nlogger = logging.getLogger(__name__)\n\nasync def trigger_collection():\n    try:\n        async with aiohttp.ClientSession() as session:\n            logger.info(\"🚀 Triggering premium OHLC collection...\")\n            \n            # Call the unified OHLC collector endpoint\n            async with session.post(\n                'http://unified-ohlc-collector.crypto-collectors.svc.cluster.local:8010/collect',\n                json={\n                    'days': 30,\n                    'force_update': False,\n                    'use_premium': True\n                }\n            ) as response:\n                if response.status == 200:\n                    result = await response.json()\n                    logger.info(f\"✅ Collection successful: {result}\")\n                else:\n                    logger.error(f\"❌ Collection failed: {response.status}\")\n                    \n    except Exception as e:\n        logger.error(f\"Error in collection job: {e}\")\n\nasyncio.run(trigger_collection())\n"]}]}}}}}}