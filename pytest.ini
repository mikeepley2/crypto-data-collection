[tool:pytest]
minversion = 6.0
addopts = 
    -v 
    --strict-markers 
    --strict-config
    --tb=short
    --cov=services 
    --cov=shared
    --cov-report=term-missing
    --cov-report=html:test-results/coverage
    --junitxml=test-results/results.xml
    --durations=10
    --color=yes
    --showlocals
    --disable-warnings

testpaths = tests

markers =
    unit: Unit tests with mocked dependencies (fast, use mock models)
    integration: Integration tests requiring real databases and APIs
    endpoint: Endpoint testing for API routes and template endpoints
    slow: Tests that take more than 30 seconds
    real_api: Tests that make actual API calls to external services
    database: Tests that require database connections
    redis: Tests that require Redis connections
    docker: Tests that require Docker containers
    performance: Performance and load testing
    auth: Authentication and authorization testing
    websocket: WebSocket functionality testing
    smoke: Basic smoke tests for quick validation
    coingecko: Tests requiring CoinGecko API
    fred: Tests requiring FRED API
    guardian: Tests requiring Guardian News API
    cleanup: Tests that clean up test data
    asyncio: Asynchronous tests using asyncio
    benchmark: Performance benchmark tests
    load: Load testing for high-volume scenarios
    ml_models: Tests involving ML model loading (environment-aware)
    mock_models: Tests using mock ML models only
    real_models: Tests requiring actual ML model downloads

python_files = test_*.py *_test.py
python_classes = Test*
python_functions = test_*

# Timeout for individual tests (5 minutes for API tests, 10 minutes for model downloads)
timeout = 600

# Environment variables for tests
env =
    ENVIRONMENT=test
    TESTING=true
    PYTHONPATH=.
    LOG_LEVEL=WARNING
    MODEL_CACHE_DIR={envtmpdir}/test_models
    
# Async test configuration
asyncio_mode = auto

# Filter warnings from dependencies
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
    ignore::FutureWarning:transformers.*
    ignore::UserWarning:torch.*
    ignore::UserWarning:huggingface_hub.*